---
phase: 02-recoverability-diagnostics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/analyzer.py
  - src/config.py
  - tests/test_analyzer.py
autonomous: true
requirements:
  - SYNC-01
  - SYNC-02
user_setup: []
must_haves:
  truths:
    - "Large runtime mismatches are classified into recoverability-aware categories instead of collapsing immediately into a generic terminal mismatch"
    - "Diagnosis output carries enough structured evidence for downstream routing without pretending the candidate is already merge-safe"
  artifacts:
    - path: "src/analyzer.py"
      provides: "Expanded recoverability-aware diagnosis contract"
      contains: "recoverability"
    - path: "tests/test_analyzer.py"
      provides: "Unit coverage for recoverable versus terminal large-diff diagnosis"
      contains: "recoverability"
  key_links:
    - from: "src/analyzer.py"
      to: "tests/test_analyzer.py"
      via: "New diagnosis categories and fields are asserted directly by unit tests"
      pattern: "recoverability|terminal"
---

<objective>
Redesign the sync diagnosis contract so Phase 2 can tell recoverable, ambiguous, and terminal mismatch cases apart before the trigger decides to fall back.

Purpose: The current duration-driven `CUT_MISMATCH` path discards rare PT-BR candidates too early. This plan creates the richer diagnosis vocabulary and metadata that the rest of the phase will consume.
Output: Updated diagnosis/config code plus focused unit tests proving recoverable-versus-terminal classification for large runtime gaps.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-recoverability-diagnostics/02-CONTEXT.md
@.planning/phases/02-recoverability-diagnostics/02-RESEARCH.md
@.planning/phases/02-recoverability-diagnostics/02-VALIDATION.md
@src/analyzer.py
@src/config.py
@tests/test_analyzer.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand analyzer diagnosis semantics for recoverability</name>
  <files>src/analyzer.py, src/config.py</files>
  <read_first>src/analyzer.py, src/config.py, .planning/phases/02-recoverability-diagnostics/02-CONTEXT.md, .planning/phases/02-recoverability-diagnostics/02-RESEARCH.md</read_first>
  <action>Update `src/analyzer.py` so `diagnose_sync()` returns an explicit recoverability-aware contract for large runtime mismatches. Keep `sync_ok`, but add structured fields such as `recoverability`, `terminal`, and a richer `category` set that can represent at least fixed offset, intro/outro divergence, drift suspicion, structurally incompatible alternate cut, and unresolved recoverable ambiguity. Preserve existing evidence fields (`diff`, `runtime_*`, `offset_estimate`, `offset_confidence`) and add any additional reasoning strings needed for downstream routing. In `src/config.py`, add only the minimum new diagnostics knobs required to support this classification cleanly, such as a configurable edge-divergence threshold or recoverability window, without introducing a new dependency or recovery engine.</action>
  <verify>Get-Content src\analyzer.py</verify>
  <acceptance_criteria>
    - src/analyzer.py contains "recoverability"
    - src/analyzer.py contains "terminal"
    - src/analyzer.py contains a non-terminal large-diff category beyond "OFFSET_SUSPECTED"
    - src/config.py contains any new diagnostics knob referenced by src/analyzer.py
  </acceptance_criteria>
  <done>The analyzer exposes a recoverability-aware diagnosis structure that downstream code can route on.</done>
</task>

<task type="auto">
  <name>Task 2: Add unit tests for recoverable versus terminal large-diff cases</name>
  <files>tests/test_analyzer.py</files>
  <read_first>tests/test_analyzer.py, src/analyzer.py, .planning/phases/02-recoverability-diagnostics/02-CONTEXT.md</read_first>
  <action>Extend `tests/test_analyzer.py` with explicit large-diff diagnosis cases covering a recoverable edge-divergence mismatch, a terminal structurally incompatible mismatch, and an ambiguous-but-preserved mismatch if the final analyzer contract includes that state. Assert exact category names or exact `recoverability` / `terminal` field values rather than vague booleans so regressions in taxonomy are caught immediately. Keep the tests isolated through mocked durations/config exactly like the current analyzer tests.</action>
  <verify>pytest -q tests/test_analyzer.py</verify>
  <acceptance_criteria>
    - tests/test_analyzer.py contains "recoverability"
    - tests/test_analyzer.py contains at least one assertion for a terminal large-diff result
    - tests/test_analyzer.py contains at least one assertion for a non-terminal large-diff result
    - `pytest -q tests/test_analyzer.py` exits 0
  </acceptance_criteria>
  <done>Analyzer tests prove that large mismatches are no longer all treated as the same failure mode.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest -q tests/test_analyzer.py` passes
- [ ] `Get-Content src\analyzer.py` shows explicit recoverability semantics
- [ ] New analyzer fields/categories are asserted by tests, not just logged
</verification>

<success_criteria>

- All tasks completed
- Analyzer exposes recoverability-aware diagnosis semantics
- Unit tests lock the new large-diff taxonomy in place
- No new recovery mechanics are attempted yet in this plan
  </success_criteria>

<output>
After completion, create `.planning/phases/02-recoverability-diagnostics/02-01-SUMMARY.md`
</output>
