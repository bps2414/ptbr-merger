---
phase: 02-recoverability-diagnostics
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - src/audio_fingerprint.py
  - src/trigger.py
  - tests/test_trigger.py
autonomous: true
requirements:
  - SYNC-01
  - SYNC-03
user_setup: []
must_haves:
  truths:
    - "Trigger routing preserves plausibly recoverable candidates instead of sending them straight to terminal fallback"
    - "Fingerprint evidence can strengthen diagnosis without Phase 2 pretending to perform the actual recovery step"
  artifacts:
    - path: "src/trigger.py"
      provides: "Recoverability-aware orchestration routing"
      contains: "recoverability"
    - path: "tests/test_trigger.py"
      provides: "Routing coverage for preserved versus terminal large-diff candidates"
      contains: "recoverability"
  key_links:
    - from: "src/trigger.py"
      to: "src/analyzer.py"
      via: "Trigger routes on the expanded diagnosis contract instead of only diff/category shortcuts"
      pattern: "terminal|recoverability"
---

<objective>
Teach the orchestration layer to respect the richer diagnosis contract so recoverable candidates stay alive for later phases instead of being discarded immediately.

Purpose: Phase 2 only matters if `run_merger()` stops translating every big mismatch into early fallback. This plan updates routing and fingerprint handoff to preserve candidates that still deserve a rescue attempt later.
Output: Updated trigger/fingerprint behavior plus orchestration tests covering preserved, ambiguous, and terminal mismatch paths.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-recoverability-diagnostics/02-CONTEXT.md
@.planning/phases/02-recoverability-diagnostics/02-RESEARCH.md
@src/analyzer.py
@src/audio_fingerprint.py
@src/trigger.py
@tests/test_trigger.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Align fingerprint evidence with recoverability routing</name>
  <files>src/audio_fingerprint.py, src/trigger.py</files>
  <read_first>src/audio_fingerprint.py, src/trigger.py, src/analyzer.py, .planning/phases/02-recoverability-diagnostics/02-RESEARCH.md</read_first>
  <action>Adjust `src/audio_fingerprint.py` and the fingerprint-handling branch in `src/trigger.py` so multi-position evidence reinforces the new recoverability model instead of collapsing back into legacy binary outcomes. Keep the current FFT-based approach, but ensure low-confidence, drift-suspected, and edge-divergence style evidence can map cleanly into non-terminal or terminal routing according to the new analyzer contract. Do not implement actual offset mux recovery here; only produce the evidence and state transitions needed to preserve the candidate for later work.</action>
  <verify>Get-Content src\trigger.py</verify>
  <acceptance_criteria>
    - src/trigger.py contains routing based on diagnosis recoverability or terminal state
    - src/audio_fingerprint.py still contains the multi-position measurement flow
    - src/trigger.py does not mark a recoverable large-diff candidate as immediate success solely because it was preserved
  </acceptance_criteria>
  <done>Fingerprint evidence and trigger routing speak the same recoverability language.</done>
</task>

<task type="auto">
  <name>Task 2: Add orchestration tests for preserve-versus-fallback behavior</name>
  <files>tests/test_trigger.py</files>
  <read_first>tests/test_trigger.py, src/trigger.py, src/analyzer.py, .planning/phases/02-recoverability-diagnostics/02-CONTEXT.md</read_first>
  <action>Extend `tests/test_trigger.py` with scenarios where a large-diff diagnosis is recoverable or ambiguous and therefore must not immediately trigger terminal fallback, plus a contrasting scenario where the diagnosis is explicitly terminal and still must fail hard. Assert concrete effects such as which notifier status is emitted, whether fallback is triggered, and whether queue/group history records a preserved-versus-terminal path. Mirror the current mocking style used by existing trigger tests so the behavior change stays isolated and debuggable.</action>
  <verify>pytest -q tests/test_trigger.py</verify>
  <acceptance_criteria>
    - tests/test_trigger.py contains at least one preserved large-diff candidate scenario
    - tests/test_trigger.py contains at least one explicit terminal mismatch scenario
    - tests/test_trigger.py asserts whether fallback is or is not triggered
    - `pytest -q tests/test_trigger.py` exits 0
  </acceptance_criteria>
  <done>Trigger tests prove the pipeline now preserves recoverable candidates and still rejects terminal ones.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest -q tests/test_trigger.py` passes
- [ ] `Get-Content src\trigger.py` shows recoverability-aware routing before fallback
- [ ] Fingerprint handling does not accidentally implement Phase 3 recovery behavior
</verification>

<success_criteria>

- All tasks completed
- Trigger routing preserves recoverable/ambiguous candidates
- Fingerprint evidence integrates cleanly with the new diagnosis contract
- Terminal mismatch behavior remains explicit and safe
  </success_criteria>

<output>
After completion, create `.planning/phases/02-recoverability-diagnostics/02-02-SUMMARY.md`
</output>
