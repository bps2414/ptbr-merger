---
phase: 02-recoverability-diagnostics
plan: 03
type: execute
wave: 3
depends_on:
  - "02-01"
  - "02-02"
files_modified:
  - src/notifier.py
  - src/sync_intelligence.py
  - tests/test_media_integration.py
  - tests/test_sync_intelligence.py
autonomous: true
requirements:
  - SYNC-02
  - SYNC-03
user_setup: []
must_haves:
  truths:
    - "Operator-facing statuses and compatibility history distinguish recoverable diagnostics from truly incompatible cuts"
    - "Fixture-backed tests prove the richer diagnosis model does not regress real-media expectations"
  artifacts:
    - path: "src/notifier.py"
      provides: "Operator-visible taxonomy for recoverability-aware mismatch states"
      contains: "recover"
    - path: "tests/test_media_integration.py"
      provides: "Regression coverage for real-media mismatch classification"
      contains: "dual_cut_mismatch"
  key_links:
    - from: "src/notifier.py"
      to: "src/sync_intelligence.py"
      via: "Human-facing status and stored history use aligned mismatch vocabulary"
      pattern: "CUT_MISMATCH|recover"
---

<objective>
Finish Phase 2 by aligning observability and historical memory with the richer diagnosis model, then lock it down with fixture-backed regression coverage.

Purpose: Recoverability is useless if operators still see generic mismatch messages and the history layer keeps penalizing candidates as if every large diff were terminal. This plan makes the new taxonomy visible and testable end to end.
Output: Updated notifier/history semantics plus integration/regression tests covering recoverable versus incompatible mismatch behavior.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-recoverability-diagnostics/02-CONTEXT.md
@.planning/phases/02-recoverability-diagnostics/02-RESEARCH.md
@src/notifier.py
@src/sync_intelligence.py
@tests/test_media_integration.py
@tests/test_sync_intelligence.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update status taxonomy and compatibility history semantics</name>
  <files>src/notifier.py, src/sync_intelligence.py</files>
  <read_first>src/notifier.py, src/sync_intelligence.py, src/trigger.py, .planning/phases/02-recoverability-diagnostics/02-CONTEXT.md</read_first>
  <action>Revise `src/notifier.py` and `src/sync_intelligence.py` so recoverable, ambiguous, and terminal large-diff states are represented consistently across user-visible status messages and stored compatibility history. Keep existing terminal outcomes explicit, but add wording and storage semantics that distinguish "candidate preserved for recovery" from "candidate structurally incompatible". Do not overfit the wording to Discord only; the same taxonomy must remain understandable in plain log/history inspection too.</action>
  <verify>Get-Content src\notifier.py</verify>
  <acceptance_criteria>
    - src/notifier.py contains operator-visible wording for at least one recoverable large-diff state
    - src/sync_intelligence.py distinguishes recoverable versus terminal mismatch outcomes in its result handling
    - src/notifier.py still contains explicit wording for terminal incompatible cases
  </acceptance_criteria>
  <done>Observability and compatibility history now reflect the richer diagnosis taxonomy instead of flattening it.</done>
</task>

<task type="auto">
  <name>Task 2: Add integration and history regressions for recoverability diagnostics</name>
  <files>tests/test_media_integration.py, tests/test_sync_intelligence.py</files>
  <read_first>tests/test_media_integration.py, tests/test_sync_intelligence.py, src/analyzer.py, src/sync_intelligence.py</read_first>
  <action>Extend `tests/test_media_integration.py` and `tests/test_sync_intelligence.py` so the real-media cut-mismatch fixture and history scoring reflect the new recoverability-aware model. Keep at least one fixture-backed case proving a structurally incompatible mismatch still stays terminal, and add or adapt assertions so recoverable/ambiguous mismatch outcomes no longer get scored or narrated exactly like hard cut failures. Preserve existing fixture names unless changing them is necessary for clarity, and keep the tests grep-verifiable with explicit expected category/result strings.</action>
  <verify>pytest -q tests/test_media_integration.py tests/test_sync_intelligence.py && python -m compileall src tests</verify>
  <acceptance_criteria>
    - tests/test_media_integration.py contains an explicit assertion for a terminal incompatible fixture outcome
    - tests/test_sync_intelligence.py contains assertions that differentiate recoverable versus terminal mismatch history behavior
    - `pytest -q tests/test_media_integration.py tests/test_sync_intelligence.py` exits 0
    - `python -m compileall src tests` exits 0
  </acceptance_criteria>
  <done>Regression coverage locks operator messaging, history semantics, and fixture behavior to the new taxonomy.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest -q tests/test_media_integration.py tests/test_sync_intelligence.py` passes
- [ ] `python -m compileall src tests` passes
- [ ] Notifier and history semantics use the same recoverability-aware vocabulary
</verification>

<success_criteria>

- All tasks completed
- Recoverability states are visible to operators and stored meaningfully in history
- Real-media and history regressions protect the new diagnosis model
- Phase 2 ends with diagnosis/routing/observability aligned for Phase 3 recovery work
  </success_criteria>

<output>
After completion, create `.planning/phases/02-recoverability-diagnostics/02-03-SUMMARY.md`
</output>
