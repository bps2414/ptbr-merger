---
phase: 04-assisted-recovery-and-outcomes
plan: 03
type: execute
wave: 3
depends_on:
  - "04-01"
  - "04-02"
files_modified:
  - tests/test_trigger.py
  - tests/test_media_integration.py
  - tests/test_notifier.py
  - tests/test_tools.py
  - tests/test_sync_intelligence.py
  - tests/support/media_fixtures.py
  - tests/test_data/media_fixtures/manifest.json
autonomous: true
requirements:
  - TEST-02
  - MAN-01
  - MAN-02
  - OBS-02
user_setup: []
must_haves:
  truths:
    - "Manual recovery can succeed through the same safety gate when the operator supplies a valid conservative recipe"
    - "Large-diff candidates that are structurally incompatible are still rejected safely even after the manual path exists"
    - "Cleanup and replacement semantics stay safe when manual recovery fails"
  artifacts:
    - path: "tests/test_trigger.py"
      provides: "Orchestration proof for manual success and safe rejection"
      contains: "manual_recovery"
    - path: "tests/test_media_integration.py"
      provides: "Fixture-backed proof that manual flow does not weaken rejection safety"
      contains: "manual"
    - path: "tests/test_data/media_fixtures/manifest.json"
      provides: "Documented expectations for any manual-specific fixture scenario"
      contains: "manual"
  key_links:
    - from: "tests/test_media_integration.py"
      to: "src/trigger.py"
      via: "Fixture-backed tests prove the manual path obeys the same safety boundary as production orchestration"
      pattern: "manual"
---

<objective>
Lock Phase 4 with orchestration and fixture-backed proof that manual recovery helps rare cases without making unsafe candidates slip through.
Output: automated regressions for manual success, manual failure, and safe rejection of structurally incompatible sources.
</objective>

<context>
@.planning/phases/04-assisted-recovery-and-outcomes/04-CONTEXT.md
@.planning/phases/04-assisted-recovery-and-outcomes/04-RESEARCH.md
@tests/test_trigger.py
@tests/test_media_integration.py
@tests/support/media_fixtures.py
@tests/test_data/media_fixtures/manifest.json
@tests/test_notifier.py
@tests/test_tools.py
@tests/test_sync_intelligence.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add orchestration regressions for manual success and manual-safe failure</name>
  <files>tests/test_trigger.py, tests/test_notifier.py, tests/test_tools.py, tests/test_sync_intelligence.py</files>
  <read_first>tests/test_trigger.py, tests/test_notifier.py, tests/test_tools.py, tests/test_sync_intelligence.py, src/trigger.py, src/notifier.py</read_first>
  <action>Add explicit regressions that prove: a manual recovery request with valid conservative parameters can finish as `MANUAL_RECOVERY_SUCCESS`; a manual recovery request that fails post-check or mux validation ends as `MANUAL_RECOVERY_FAILED`; and the failed manual path still preserves safe cleanup/replacement behavior (no invalid replacement, no misleading terminal structural status). Name the tests around `manual_recovery` so the quick validation commands in `04-VALIDATION.md` can target them directly.</action>
  <verify>pytest -q tests/test_trigger.py tests/test_notifier.py tests/test_tools.py tests/test_sync_intelligence.py</verify>
  <acceptance_criteria>
    - tests/test_trigger.py contains `MANUAL_RECOVERY_SUCCESS`
    - tests/test_trigger.py contains `MANUAL_RECOVERY_FAILED`
    - tests/test_trigger.py proves failed manual recovery does not replace the original 4K with an invalid result
    - tests/test_notifier.py asserts manual terminal wording
    - tests/test_tools.py or tests/test_sync_intelligence.py asserts manual failure stays distinct from structural incompatibility
  </acceptance_criteria>
  <done>The manual path is covered at the orchestration level for both good and bad outcomes.</done>
</task>

<task type="auto">
  <name>Task 2: Add fixture-backed rejection proof and run the full phase gate</name>
  <files>tests/test_media_integration.py, tests/support/media_fixtures.py, tests/test_data/media_fixtures/manifest.json, tests/test_trigger.py</files>
  <read_first>tests/test_media_integration.py, tests/support/media_fixtures.py, tests/test_data/media_fixtures/manifest.json, src/merger.py, src/analyzer.py, src/trigger.py</read_first>
  <action>Use the existing synthetic media corpus, extending `tests/support/media_fixtures.py` and `tests/test_data/media_fixtures/manifest.json` only if required, to add fixture-backed proof for two Phase 4 outcomes: a conservative manual recipe that can succeed on a recoverable case, and a structurally incompatible large-diff case that still fails safely even when manual recovery is requested. Finish by running `pytest -q tests/test_trigger.py tests/test_notifier.py tests/test_tools.py tests/test_sync_intelligence.py tests/test_media_integration.py && python -m compileall src tests`.</action>
  <verify>pytest -q tests/test_trigger.py tests/test_notifier.py tests/test_tools.py tests/test_sync_intelligence.py tests/test_media_integration.py && python -m compileall src tests</verify>
  <acceptance_criteria>
    - tests/test_media_integration.py contains at least one manual success scenario
    - tests/test_media_integration.py contains at least one manual rejection scenario
    - manifest.json documents any new manual-specific fixture expectation explicitly
    - the full phase suite and compile command exit 0
  </acceptance_criteria>
  <done>Phase 4 closes with proof that manual recovery expands operator control without weakening safe rejection.</done>
</task>

</tasks>

<verification>
- [ ] `pytest -q tests/test_trigger.py tests/test_notifier.py tests/test_tools.py tests/test_sync_intelligence.py tests/test_media_integration.py` passes
- [ ] `python -m compileall src tests` passes
- [ ] manual success and manual-safe rejection both have automated proof
- [ ] structurally incompatible candidates are still rejected safely after Phase 4 exists
</verification>

<output>
After completion, create `.planning/phases/04-assisted-recovery-and-outcomes/04-03-SUMMARY.md`
</output>
