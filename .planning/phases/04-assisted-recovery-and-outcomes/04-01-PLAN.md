---
phase: 04-assisted-recovery-and-outcomes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/trigger.py
  - src/config.py
  - config.example.yml
  - tests/test_trigger.py
  - tests/test_config.py
autonomous: true
requirements:
  - MAN-01
  - MAN-02
  - MAN-03
user_setup: []
must_haves:
  truths:
    - "The operator can start an explicit manual recovery attempt without editing source code"
    - "Manual parameters are normalized and persisted as concrete metadata for reproducible retries"
    - "Manual mode may force an attempt but cannot skip final validation"
  artifacts:
    - path: "src/trigger.py"
      provides: "Explicit manual recovery CLI contract and orchestration bridge"
      contains: "manual-recovery"
    - path: "config.example.yml"
      provides: "Documented manual recovery knobs"
      contains: "preserve_failed_artifacts"
  key_links:
    - from: "src/trigger.py"
      to: "src/config.py"
      via: "Manual request parsing uses typed config defaults and runtime paths"
      pattern: "manual"
---

<objective>
Introduce the explicit manual recovery contract and thread it through the existing trigger/recovery path without weakening the safety gate.
Output: operator-invoked CLI/manual flow plus persisted request metadata that can be replayed.
</objective>

<context>
@.planning/phases/04-assisted-recovery-and-outcomes/04-CONTEXT.md
@.planning/phases/04-assisted-recovery-and-outcomes/04-RESEARCH.md
@src/trigger.py
@src/config.py
@config.example.yml
@tests/test_trigger.py
@tests/test_config.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add an explicit manual recovery CLI contract</name>
  <files>src/trigger.py, src/config.py, config.example.yml, tests/test_trigger.py, tests/test_config.py</files>
  <read_first>src/trigger.py, src/config.py, config.example.yml, README.md, .planning/phases/04-assisted-recovery-and-outcomes/04-CONTEXT.md</read_first>
  <action>Extend `parse_args()` in `src/trigger.py` with explicit operator flags for Phase 4: `--manual-recovery`, `--tmdb-id`, `--candidate-index`, `--force-offset-seconds`, `--trim-start-seconds`, `--trim-end-seconds`, `--reuse-last-recovery`, and `--preserve-recovery-artifacts`. Add typed config support in `src/config.py` and `config.example.yml` for any defaults needed by this flow, keeping names concrete and local to manual recovery. Reject invalid combinations early, such as `--manual-recovery` without `--tmdb-id` or without either a resolved candidate path (`--qbit-path`) or a reusable prior attempt.</action>
  <verify>pytest -q tests/test_trigger.py tests/test_config.py</verify>
  <acceptance_criteria>
    - src/trigger.py contains `--manual-recovery`
    - src/trigger.py contains `--tmdb-id`
    - src/trigger.py contains `--force-offset-seconds`
    - src/trigger.py contains `--trim-start-seconds`
    - src/trigger.py contains `--trim-end-seconds`
    - tests/test_trigger.py covers invalid and valid manual CLI combinations
    - config.example.yml documents any new manual recovery defaults explicitly
  </acceptance_criteria>
  <done>The operator has a real CLI contract for manual recovery instead of needing source edits.</done>
</task>

<task type="auto">
  <name>Task 2: Normalize, persist, and execute manual recovery requests safely</name>
  <files>src/trigger.py, tests/test_trigger.py</files>
  <read_first>src/trigger.py, src/queue_manager.py, src/history_manager.py, src/analyzer.py, src/merger.py, tests/test_trigger.py</read_first>
  <action>Add a normalized manual request payload inside `src/trigger.py` with exact persisted fields: `manual_recovery`, `manual_request_id`, `manual_candidate_index`, `manual_force_offset_seconds`, `manual_trim_start_seconds`, `manual_trim_end_seconds`, `manual_reuse_last_recovery`, `manual_preserve_artifacts`, and `manual_source_path`. Thread that payload into `run_merger()` so manual mode can choose `manual-offset` or `manual-edge-trim` strategies from operator parameters while still calling `analyzer.validate_recovery_attempt()` and `merger.validate_and_replace()` before success. Record the manual request metadata in queue/history context so the exact retry recipe survives failure and can be inspected later.</action>
  <verify>pytest -q tests/test_trigger.py</verify>
  <acceptance_criteria>
    - src/trigger.py contains `manual_request_id`
    - src/trigger.py contains `manual_force_offset_seconds`
    - src/trigger.py contains `manual_trim_end_seconds`
    - manual recovery paths still call `validate_recovery_attempt`
    - manual recovery paths still call `validate_and_replace`
    - tests/test_trigger.py proves a manual attempt can reuse explicit offset or trim values without bypassing validation
  </acceptance_criteria>
  <done>Manual attempts run through the existing recovery engine with persisted, reproducible parameters.</done>
</task>

</tasks>

<verification>
- [ ] `pytest -q tests/test_trigger.py tests/test_config.py` passes
- [ ] explicit manual recovery invocation exists without requiring source edits
- [ ] manual request metadata is persisted with concrete replay fields
- [ ] validation remains mandatory for manual attempts
</verification>

<output>
After completion, create `.planning/phases/04-assisted-recovery-and-outcomes/04-01-SUMMARY.md`
</output>
