---
phase: 04-assisted-recovery-and-outcomes
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - src/notifier.py
  - src/sync_intelligence.py
  - src/tools/status.py
  - src/tools/refresh_webhook.py
  - README.md
  - tests/test_notifier.py
  - tests/test_sync_intelligence.py
  - tests/test_tools.py
  - tests/test_trigger.py
autonomous: true
requirements:
  - OBS-01
  - OBS-02
  - MAN-03
user_setup: []
must_haves:
  truths:
    - "Notifications, queue/history refresh, and compatibility history all show manual states explicitly"
    - "Manual recovery failure is distinguishable from structural incompatibility and from automatic recovery failure"
    - "Persisted manual metadata is visible through operator tooling, not buried only in raw JSON"
  artifacts:
    - path: "src/notifier.py"
      provides: "Operator-facing manual recovery vocabulary"
      contains: "MANUAL_RECOVERY"
    - path: "src/tools/status.py"
      provides: "Runtime snapshot that exposes manual request metadata and outcomes"
      contains: "manual"
    - path: "README.md"
      provides: "Documented manual recovery commands"
      contains: "--manual-recovery"
  key_links:
    - from: "src/tools/refresh_webhook.py"
      to: "src/notifier.py"
      via: "Manual terminal states can be rebuilt from persisted queue/history data"
      pattern: "MANUAL_RECOVERY"
---

<objective>
Expose clear manual outcome semantics across notifications, runtime tools, and compatibility history so operators can tell what happened and retry intentionally.
Output: aligned manual status vocabulary plus documented/manual-visible metadata.
</objective>

<context>
@.planning/phases/04-assisted-recovery-and-outcomes/04-CONTEXT.md
@.planning/phases/04-assisted-recovery-and-outcomes/04-RESEARCH.md
@src/notifier.py
@src/sync_intelligence.py
@src/tools/status.py
@src/tools/refresh_webhook.py
@README.md
@tests/test_notifier.py
@tests/test_sync_intelligence.py
@tests/test_tools.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add shared manual outcome vocabulary across notifier and refresh paths</name>
  <files>src/notifier.py, src/tools/refresh_webhook.py, src/sync_intelligence.py, tests/test_notifier.py, tests/test_sync_intelligence.py, tests/test_trigger.py</files>
  <read_first>src/notifier.py, src/tools/refresh_webhook.py, src/sync_intelligence.py, src/trigger.py, tests/test_notifier.py, tests/test_sync_intelligence.py</read_first>
  <action>Add explicit status/result names for manual flow states: `MANUAL_RECOVERY_PENDING`, `MANUAL_RECOVERY_RUNNING`, `MANUAL_RECOVERY_FAILED`, and `MANUAL_RECOVERY_SUCCESS`. Wire those names into notifier labels/messages/embed fields, refresh-webhook reconstruction, and compatibility/group-history result handling so manual failure is not scored or presented as `CUT_MISMATCH`, `RUNTIME_INCOMPATIBLE`, or `AUTO_RECOVERY_FAILED`. Preserve the existing structural mismatch statuses for genuinely unrecoverable candidates.</action>
  <verify>pytest -q tests/test_notifier.py tests/test_sync_intelligence.py tests/test_trigger.py</verify>
  <acceptance_criteria>
    - src/notifier.py contains `MANUAL_RECOVERY_PENDING`
    - src/notifier.py contains `MANUAL_RECOVERY_FAILED`
    - src/notifier.py contains `MANUAL_RECOVERY_SUCCESS`
    - src/tools/refresh_webhook.py can rebuild manual terminal statuses from history
    - src/sync_intelligence.py keeps manual failure separate from cut/runtime failure buckets
    - tests prove manual failure does not collapse into structural incompatibility wording
  </acceptance_criteria>
  <done>Every operator-facing surface now names manual states honestly.</done>
</task>

<task type="auto">
  <name>Task 2: Surface manual metadata in status tooling and docs</name>
  <files>src/tools/status.py, README.md, tests/test_tools.py</files>
  <read_first>src/tools/status.py, README.md, tests/test_tools.py, .planning/phases/04-assisted-recovery-and-outcomes/04-CONTEXT.md</read_first>
  <action>Update `src/tools/status.py` so the runtime snapshot exposes manual request fields such as `manual_request_id`, `manual_candidate_index`, `manual_force_offset_seconds`, `manual_trim_start_seconds`, `manual_trim_end_seconds`, and the latest manual outcome. Document the supported operator commands in `README.md` with at least one concrete example using `python src/trigger.py --manual-recovery --tmdb-id ... --qbit-path ...` and one example that includes offset or trim overrides.</action>
  <verify>pytest -q tests/test_tools.py</verify>
  <acceptance_criteria>
    - src/tools/status.py contains `manual_request_id`
    - src/tools/status.py contains `manual_force_offset_seconds`
    - README.md contains `--manual-recovery`
    - README.md contains `--tmdb-id`
    - README.md contains either `--force-offset-seconds` or `--trim-end-seconds`
    - tests/test_tools.py asserts manual metadata appears in the status snapshot
  </acceptance_criteria>
  <done>Operators can inspect and invoke manual recovery without spelunking raw implementation details.</done>
</task>

</tasks>

<verification>
- [ ] `pytest -q tests/test_notifier.py tests/test_tools.py tests/test_sync_intelligence.py tests/test_trigger.py` passes
- [ ] manual outcome wording is explicit in notifier and refresh flows
- [ ] status tooling surfaces replayable manual metadata
- [ ] README documents concrete manual recovery commands
</verification>

<output>
After completion, create `.planning/phases/04-assisted-recovery-and-outcomes/04-02-SUMMARY.md`
</output>
