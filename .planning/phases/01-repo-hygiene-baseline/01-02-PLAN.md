---
phase: 01-repo-hygiene-baseline
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - .gitignore
autonomous: true
requirements:
  - HYGN-01
  - HYGN-02
  - HYGN-03
user_setup: []
must_haves:
  truths:
    - "Runtime state, logs, caches, and local tooling artifacts are ignored by default"
    - "Planning artifacts remain intentionally trackable while local operator files stay local-only"
    - "The ignore policy matches the audited repository boundary instead of guessing"
  artifacts:
    - path: ".gitignore"
      provides: "Repository hygiene rules"
      min_lines: 20
      contains: ".planning/"
    - path: ".gitignore"
      provides: "Runtime and cache exclusions"
      contains: "__pycache__"
    - path: ".gitignore"
      provides: "Local operator and tooling exclusions"
      contains: "config.yml"
  key_links:
    - from: ".gitignore"
      to: ".planning/phases/01-repo-hygiene-baseline/01-repo-boundary-audit.md"
      via: "Ignore rules implement audited boundary decisions"
      pattern: "config\\.yml|queue\\.json|\\.codex"
---

<objective>
Restore the repository hygiene rules by creating or correcting `.gitignore` to match the audited tracked/local boundary.

Purpose: Make `git status` useful again and stop runtime, cache, and local-tooling noise from polluting source control.
Output: A `.gitignore` file that preserves tracked project artifacts while excluding local/operator/generated files.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-repo-hygiene-baseline/01-CONTEXT.md
@.planning/phases/01-repo-hygiene-baseline/01-repo-boundary-audit.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Recreate .gitignore with explicit hygiene groups</name>
  <files>.gitignore</files>
  <read_first>.planning/phases/01-repo-hygiene-baseline/01-CONTEXT.md, .planning/phases/01-repo-hygiene-baseline/01-repo-boundary-audit.md, config.example.yml</read_first>
  <action>Create a new `.gitignore` file if missing, or replace it with grouped ignore sections that match the locked Phase 1 policy. Include sections for Python cache/build noise (`__pycache__/`, `*.py[cod]`, `.pytest_cache/`), local configuration (`config.yml`, `.env`, `.env.*` but keep `.env.example` tracked), runtime state/logs (`queue.json`, `retry_queue.json`, `history.json`, `group_history.json`, `ptbrmerger.log`), local tooling/dev artifacts (`.codex/`, `.codex-repro-readonly/`, `.codex-repro-write/`, `.runtime-archive/`, `tmp/`, `get-shit-done-cc-1.28.0.tgz`), and standard OS/editor noise if useful. Do not ignore `.planning/`, `config.example.yml`, `README.md`, or source/test/script directories.</action>
  <verify>Get-Content .gitignore</verify>
  <acceptance_criteria>
    - .gitignore contains "__pycache__/"
    - .gitignore contains "config.yml"
    - .gitignore contains "queue.json"
    - .gitignore contains ".codex/"
    - .gitignore contains "tmp/"
    - .gitignore does not contain a rule that ignores ".planning/"
    - .gitignore does not contain a rule that ignores "config.example.yml"
  </acceptance_criteria>
  <done>.gitignore exists and captures the complete tracked/local boundary without excluding required project artifacts.</done>
</task>

<task type="auto">
  <name>Task 2: Validate ignore policy against the current workspace</name>
  <files>.gitignore</files>
  <read_first>.gitignore, .planning/phases/01-repo-hygiene-baseline/01-repo-boundary-audit.md</read_first>
  <action>Run a post-change sanity check against the current workspace state. Confirm that the `.gitignore` patterns cover the existing runtime JSON files, log file, cache directories, and local tooling directories identified in the audit. If the policy misses any current noisy artifact class, update `.gitignore` immediately so the final file matches the real workspace instead of an idealized repo.</action>
  <verify>git -c safe.directory=D:/ptbr-merger status --short</verify>
  <acceptance_criteria>
    - git status output no longer reports "__pycache__" paths as untracked changes
    - git status output no longer reports ".codex-repro-readonly/" as untracked changes
    - git status output no longer reports "tmp/" as untracked changes
    - git status output still allows intentional project files like ".planning/" documents to appear when modified
  </acceptance_criteria>
  <done>The ignore policy has been checked against the actual workspace and covers the noisy artifacts currently present.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `Get-Content .gitignore` shows grouped sections for caches, local config, runtime files, and tooling artifacts
- [ ] `git -c safe.directory=D:/ptbr-merger status --short` no longer lists ignored noise classes from the current workspace
- [ ] `.planning/` remains trackable by policy
</verification>

<success_criteria>

- All tasks completed
- `.gitignore` exists and matches the audited boundary
- Runtime/operator data stays local-only without blocking normal operations
- Planning docs remain intentionally trackable
  </success_criteria>

<output>
After completion, create `.planning/phases/01-repo-hygiene-baseline/01-02-SUMMARY.md`
</output>
