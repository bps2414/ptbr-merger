---
phase: 01-repo-hygiene-baseline
plan: 03
type: execute
wave: 3
depends_on:
  - "01-02"
files_modified:
  - README.md
autonomous: true
requirements:
  - HYGN-02
  - HYGN-03
user_setup: []
must_haves:
  truths:
    - "Contributors can read the README and understand what is tracked versus local-only"
    - "The documented hygiene policy matches the implemented ignore rules"
  artifacts:
    - path: "README.md"
      provides: "Repository hygiene guidance for contributors/operators"
      min_lines: 150
      contains: "## Repository Hygiene"
    - path: ".gitignore"
      provides: "Implemented ignore policy referenced by docs"
      contains: "config.yml"
  key_links:
    - from: "README.md"
      to: ".gitignore"
      via: "Documentation explains the same tracked/local boundary implemented in ignore rules"
      pattern: "config\\.yml|queue\\.json|\\.planning/"
---

<objective>
Document the repository hygiene boundary in the README so the cleanup policy is obvious to future contributors and operators.

Purpose: Prevent the workspace from drifting back into source-control noise by making the tracked/local split explicit in the main project documentation.
Output: A short `Repository Hygiene` section in README aligned with the actual `.gitignore` policy.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-repo-hygiene-baseline/01-CONTEXT.md
@.planning/phases/01-repo-hygiene-baseline/01-repo-boundary-audit.md
@.gitignore
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a concise repository hygiene section to README</name>
  <files>README.md</files>
  <read_first>README.md, .gitignore, .planning/phases/01-repo-hygiene-baseline/01-CONTEXT.md, .planning/phases/01-repo-hygiene-baseline/01-repo-boundary-audit.md</read_first>
  <action>Insert a short `## Repository Hygiene` section in `README.md` near the operational/configuration guidance. Explain that `.planning/`, code, tests, scripts, and tracked docs stay versioned, while `config.yml`, runtime JSON ledgers (`queue.json`, `retry_queue.json`, `history.json`, `group_history.json`), `ptbrmerger.log`, caches, and local tooling directories remain local-only. Keep the tone pragmatic and brief; this is not a policy essay. Mention that contributors should use `config.example.yml` as the tracked template and keep real local configuration in `config.yml`.</action>
  <verify>Get-Content README.md</verify>
  <acceptance_criteria>
    - README.md contains "## Repository Hygiene"
    - README.md contains ".planning/"
    - README.md contains "config.yml"
    - README.md contains "queue.json"
    - README.md contains "config.example.yml"
  </acceptance_criteria>
  <done>README contains a concise hygiene section that spells out the tracked/local boundary with repository-specific examples.</done>
</task>

<task type="auto">
  <name>Task 2: Cross-check docs against implemented ignore rules</name>
  <files>README.md</files>
  <read_first>README.md, .gitignore</read_first>
  <action>Read the final `.gitignore` and make sure the new README section names only policies that are actually implemented. If the docs mention a local-only file or directory class that is missing from `.gitignore`, update the README wording so it accurately reflects the enforced policy instead of promising behavior the repo does not implement.</action>
  <verify>Select-String -Path README.md -Pattern "Repository Hygiene|config.yml|queue.json|.planning/"</verify>
  <acceptance_criteria>
    - README.md hygiene section references only files or directories that exist in .gitignore or are intentionally tracked
    - README.md does not say ".planning/" is local-only
    - README.md states that `config.example.yml` is the tracked template and `config.yml` is local-only
  </acceptance_criteria>
  <done>README and .gitignore tell the same story about the repo boundary.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `Select-String -Path README.md -Pattern "## Repository Hygiene"` returns a match
- [ ] The new README section mentions `.planning/`, `config.yml`, and runtime JSON ledgers
- [ ] The documented boundary matches the implemented `.gitignore`
</verification>

<success_criteria>

- All tasks completed
- README clearly documents the tracked/local split
- Documentation is concise and accurate
- Future contributors can understand the repo hygiene policy without reading planning files
  </success_criteria>

<output>
After completion, create `.planning/phases/01-repo-hygiene-baseline/01-03-SUMMARY.md`
</output>
