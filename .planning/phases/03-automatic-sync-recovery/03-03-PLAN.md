---
phase: 03-automatic-sync-recovery
plan: 03
type: execute
wave: 3
depends_on:
  - "03-01"
  - "03-02"
files_modified:
  - tests/support/media_fixtures.py
  - tests/test_data/media_fixtures/manifest.json
  - tests/test_media_integration.py
  - tests/test_trigger.py
autonomous: true
requirements:
  - TEST-01
  - SAFE-01
user_setup: []
must_haves:
  truths:
    - "At least one large-diff candidate becomes valid after conservative automatic recovery"
    - "Structurally incompatible candidates still fail safely beside the new recovery success case"
  artifacts:
    - path: "tests/support/media_fixtures.py"
      provides: "fixture corpus expanded for recoverable edge divergence"
      contains: "edge"
    - path: "tests/test_media_integration.py"
      provides: "end-to-end proof of large-diff recovery success"
      contains: "recovery"
  key_links:
    - from: "tests/test_media_integration.py"
      to: "src/trigger.py"
      via: "Fixture-backed tests prove the automatic path works on real media patterns"
      pattern: "recovery"
---

<objective>
Lock Phase 3 with fixture-backed proof that conservative automatic recovery can succeed without weakening rejection of incompatible cuts.
Output: expanded media corpus and integration regressions proving safe automatic recovery.
</objective>

<context>
@.planning/phases/03-automatic-sync-recovery/03-CONTEXT.md
@.planning/phases/03-automatic-sync-recovery/03-RESEARCH.md
@tests/support/media_fixtures.py
@tests/test_data/media_fixtures/manifest.json
@tests/test_media_integration.py
@tests/test_trigger.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a recoverable edge-divergence media fixture</name>
  <files>tests/support/media_fixtures.py, tests/test_data/media_fixtures/manifest.json</files>
  <read_first>tests/support/media_fixtures.py, tests/test_data/media_fixtures/manifest.json, .planning/phases/03-automatic-sync-recovery/03-RESEARCH.md</read_first>
  <action>Extend the fixture corpus with a large-diff candidate that is actually recoverable through conservative edge trimming or equivalent border adjustment. Keep it distinct from the existing offset-only and structural-mismatch fixtures so the success case proves something Phase 2 could not. Update the manifest expectations accordingly.</action>
  <verify>pytest -q tests/test_media_integration.py -k recovery</verify>
  <acceptance_criteria>
    - fixture corpus contains a recoverable large-diff case distinct from offset-only
    - manifest documents the expected recovery behavior explicitly
  </acceptance_criteria>
  <done>Test data now exercises the new automatic recovery path honestly.</done>
</task>

<task type="auto">
  <name>Task 2: Add end-to-end recovery regressions and final validation pass</name>
  <files>tests/test_media_integration.py, tests/test_trigger.py</files>
  <read_first>tests/test_media_integration.py, tests/test_trigger.py, src/trigger.py, src/merger.py</read_first>
  <action>Add integration and orchestration regressions that prove at least one large-diff candidate becomes valid after automatic recovery while structurally incompatible cases still fail safely. End with the full validation suite and compile gate required for phase close.</action>
  <verify>pytest -q tests/test_trigger.py tests/test_media_integration.py tests/test_analyzer.py tests/test_sync_intelligence.py && python -m compileall src tests</verify>
  <acceptance_criteria>
    - tests prove one large-diff candidate succeeds after recovery
    - incompatible cut fixture still fails safely
    - full suite and compile pass
  </acceptance_criteria>
  <done>Phase 3 has end-to-end proof of safe automatic recovery.</done>
</task>

</tasks>

<verification>
- [ ] `pytest -q tests/test_trigger.py tests/test_media_integration.py tests/test_analyzer.py tests/test_sync_intelligence.py` passes
- [ ] `python -m compileall src tests` passes
- [ ] fixture-backed coverage proves both safe success and safe rejection
</verification>

<output>
After completion, create `.planning/phases/03-automatic-sync-recovery/03-03-SUMMARY.md`
</output>
