---
phase: 03-automatic-sync-recovery
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - src/analyzer.py
  - src/notifier.py
  - src/sync_intelligence.py
  - src/trigger.py
  - tests/test_trigger.py
  - tests/test_analyzer.py
  - tests/test_sync_intelligence.py
autonomous: true
requirements:
  - AUTO-02
  - SAFE-01
  - AUTO-03
user_setup: []
must_haves:
  truths:
    - "Automatic recovery only succeeds when validation and sync-specific post-checks both agree"
    - "Recovery failures remain explicit and explain what was attempted"
  artifacts:
    - path: "src/analyzer.py"
      provides: "post-recovery validation support"
      contains: "post-recovery"
    - path: "src/notifier.py"
      provides: "operator-facing recovery attempt outcomes"
      contains: "recovery"
  key_links:
    - from: "src/trigger.py"
      to: "src/analyzer.py"
      via: "Trigger asks analyzer to validate outputs before replacement"
      pattern: "validation"
---

<objective>
Add the safety gates and explicit observability required to trust automatic recovery outcomes.
Output: validation-aware recovery results plus updated operator/history semantics.
</objective>

<context>
@.planning/phases/03-automatic-sync-recovery/03-CONTEXT.md
@.planning/phases/03-automatic-sync-recovery/03-RESEARCH.md
@src/analyzer.py
@src/notifier.py
@src/sync_intelligence.py
@src/trigger.py
@tests/test_trigger.py
@tests/test_analyzer.py
@tests/test_sync_intelligence.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add post-recovery validation gates</name>
  <files>src/analyzer.py, src/trigger.py, tests/test_analyzer.py, tests/test_trigger.py</files>
  <read_first>src/analyzer.py, src/trigger.py, tests/test_analyzer.py, tests/test_trigger.py</read_first>
  <action>Add a post-recovery validation contract that combines final-file validation with at least one sync-specific check appropriate to the attempted strategy. Use it to block replacement when FFmpeg technically succeeded but the recovered output is still not trustworthy. Keep the checks conservative and strategy-aware rather than generic guesswork.</action>
  <verify>pytest -q tests/test_trigger.py tests/test_analyzer.py</verify>
  <acceptance_criteria>
    - automatic recovery success requires more than FFmpeg exit code
    - invalid recovered outputs are rejected before replacement
    - trigger tests prove failed validation produces explicit recovery failure outcomes
  </acceptance_criteria>
  <done>Recovery success is now gated by conservative validation, not optimism.</done>
</task>

<task type="auto">
  <name>Task 2: Align observability and history with recovery outcomes</name>
  <files>src/notifier.py, src/sync_intelligence.py, src/trigger.py, tests/test_sync_intelligence.py, tests/test_trigger.py</files>
  <read_first>src/notifier.py, src/sync_intelligence.py, src/trigger.py, tests/test_sync_intelligence.py</read_first>
  <action>Expose strategy attempt results, validation reasons, and recovery-specific failures clearly in notifier output, queue/history metadata, and compatibility history semantics. Distinguish "candidate failed automatic recovery" from "candidate is unrecoverable" so Phase 4 has an honest foundation.</action>
  <verify>pytest -q tests/test_trigger.py tests/test_sync_intelligence.py</verify>
  <acceptance_criteria>
    - notifier surfaces recovery strategy and outcome vocabulary
    - history distinguishes failed recovery from terminal incompatibility
    - trigger records recovery evidence consistently for later manual follow-up
  </acceptance_criteria>
  <done>Operators and later phases can see exactly what the automatic path tried and why it stopped.</done>
</task>

</tasks>

<verification>
- [ ] `pytest -q tests/test_trigger.py tests/test_analyzer.py tests/test_sync_intelligence.py` passes
- [ ] recovered outputs are blocked unless post-recovery validation succeeds
- [ ] observability differentiates failed recovery from unrecoverable cut mismatch
</verification>

<output>
After completion, create `.planning/phases/03-automatic-sync-recovery/03-02-SUMMARY.md`
</output>
