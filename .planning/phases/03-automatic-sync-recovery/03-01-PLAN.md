---
phase: 03-automatic-sync-recovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config.py
  - src/merger.py
  - src/trigger.py
  - tests/test_merger.py
  - tests/test_trigger.py
autonomous: true
requirements:
  - AUTO-01
  - AUTO-03
user_setup: []
must_haves:
  truths:
    - "Automatic recovery starts with a conservative strategy ladder instead of a single hard-coded offset path"
    - "Recovery attempts record which strategy was chosen and with which measurable parameters"
  artifacts:
    - path: "src/trigger.py"
      provides: "Recovery strategy orchestration before fallback"
      contains: "recovery_strategy"
    - path: "src/merger.py"
      provides: "Recovery primitives for offset and edge trimming"
      contains: "trim"
  key_links:
    - from: "src/trigger.py"
      to: "src/merger.py"
      via: "Trigger chooses recovery strategy and merger executes the media transform"
      pattern: "recovery_strategy"
---

<objective>
Introduce the conservative automatic recovery ladder and wire it into the existing preserved-candidate flow.
Output: strategy-aware trigger + merger primitives with unit/orchestration coverage.
</objective>

<context>
@.planning/phases/03-automatic-sync-recovery/03-CONTEXT.md
@.planning/phases/03-automatic-sync-recovery/03-RESEARCH.md
@src/trigger.py
@src/merger.py
@src/config.py
@tests/test_trigger.py
@tests/test_merger.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add conservative recovery primitives and config knobs</name>
  <files>src/config.py, src/merger.py</files>
  <read_first>src/config.py, src/merger.py, .planning/phases/03-automatic-sync-recovery/03-RESEARCH.md</read_first>
  <action>Add explicit configuration for automatic recovery strategies and implement the minimum media primitives needed for Phase 3: offset reuse of the existing mux path plus edge trimming support for intro/outro-style recovery. Keep the implementation FFmpeg-native and conservative; do not add aggressive stretching or multi-cut reconstruction.</action>
  <verify>pytest -q tests/test_merger.py</verify>
  <acceptance_criteria>
    - src/merger.py supports the new conservative recovery primitive(s)
    - src/config.py exposes strategy-relevant knobs instead of hard-coded magic values
    - tests/test_merger.py covers the new FFmpeg command shape
  </acceptance_criteria>
  <done>Recovery primitives exist for trigger orchestration to call safely.</done>
</task>

<task type="auto">
  <name>Task 2: Route recoverable candidates into strategy-aware automatic attempts</name>
  <files>src/trigger.py, tests/test_trigger.py</files>
  <read_first>src/trigger.py, src/analyzer.py, src/audio_fingerprint.py, tests/test_trigger.py</read_first>
  <action>Teach the trigger to route preserved candidates into the automatic recovery ladder before fallback. Strategy selection must distinguish direct offset cases from edge-trim candidates and record concrete metadata such as chosen strategy, estimated offsets, trim bounds, and whether the attempt came from recoverable versus ambiguous evidence. Keep the flow tactical inside existing orchestration unless a tiny helper extraction improves readability materially.</action>
  <verify>pytest -q tests/test_trigger.py tests/test_merger.py</verify>
  <acceptance_criteria>
    - preserved candidates can enter automatic recovery before fallback
    - trigger records a named recovery strategy and measurable attempt metadata
    - tests/test_trigger.py distinguishes offset and trim attempt routing
  </acceptance_criteria>
  <done>Automatic recovery attempts are strategy-aware and visible in orchestration tests.</done>
</task>

</tasks>

<verification>
- [ ] `pytest -q tests/test_trigger.py tests/test_merger.py` passes
- [ ] trigger routes preserved candidates into strategy-aware recovery
- [ ] no aggressive recovery behavior sneaks into Phase 3
</verification>

<output>
After completion, create `.planning/phases/03-automatic-sync-recovery/03-01-SUMMARY.md`
</output>
